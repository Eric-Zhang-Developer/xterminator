{
  "name": "@xterminator/extension",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny Chrome extension that hides the For you timeline on X/Twitter home.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.306",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
